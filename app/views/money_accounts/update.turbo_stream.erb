<% if @money_account.valid? %>
  <%= turbo_stream.replace dom_id(@money_account) do %>
    <%= render partial: "money_accounts/money_account", locals: { account: @money_account } %>
  <% end %>
<% end %>

<%= render_turbo_stream_flash_messages %>